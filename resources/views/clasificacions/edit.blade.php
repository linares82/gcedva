@extends('plantillas.admin_template')

@include('clasificacions._common')

@section('header')

	<ol class="breadcrumb">
	    <li><a href="{{ route('home') }}"><span class="glyphicon glyphicon-home" aria-hidden="true"></span></a></li>
	    <li><a href="{{ route('clasificacions.index') }}">@yield('clasificacionsAppTitle')</a></li>
	    <li><a href="{{ route('clasificacions.show', $clasificacion->id) }}">{{ $clasificacion->id }}</a></li>
	    <li class="active">Editar</li>
	</ol>

    <div class="page-header">
        <h3><i class="glyphicon glyphicon-edit"></i> @yield('clasificacionsAppTitle') / Editar {{$clasificacion->id}}</h3>
    </div>
@endsection

@section('content')
    @include('error')

    <div class="row">
        <div class="col-md-12">

            {!! Form::model($clasificacion, array('route' => array('clasificacions.update', $clasificacion->id),'method' => 'post')) !!}

@include('clasificacions._form')

                <div class="row">
                </div>

                <div class="well well-sm">
                    <button type="submit" class="btn btn-primary">Guardar</button>
                    <a class="btn btn-link pull-right" href="{{ route('clasificacions.index') }}"><i class="glyphicon glyphicon-backward"></i>  Regresar</a>
                </div>
            {!! Form::close() !!}

        </div>
    </div>
@endsection