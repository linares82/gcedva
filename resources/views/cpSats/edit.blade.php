@extends('plantillas.admin_template')

@include('cpSats._common')

@section('header')

	<ol class="breadcrumb">
	    <li><a href="{{ route('home') }}"><span class="glyphicon glyphicon-home" aria-hidden="true"></span></a></li>
	    <li><a href="{{ route('cpSats.index') }}">@yield('cpSatsAppTitle')</a></li>
	    <li><a href="{{ route('cpSats.show', $cpSat->id) }}">{{ $cpSat->id }}</a></li>
	    <li class="active">Editar</li>
	</ol>

    <div class="page-header">
        <h3><i class="glyphicon glyphicon-edit"></i> @yield('cpSatsAppTitle') / Editar {{$cpSat->id}}</h3>
    </div>
@endsection

@section('content')
    @include('error')

    <div class="row">
        <div class="col-md-12">

            {!! Form::model($cpSat, array('route' => array('cpSats.update', $cpSat->id),'method' => 'post')) !!}

@include('cpSats._form')

                <div class="row">
                </div>

                <div class="well well-sm">
                    <button type="submit" class="btn btn-primary">Guardar</button>
                    <a class="btn btn-link pull-right" href="{{ route('cpSats.index') }}"><i class="glyphicon glyphicon-backward"></i>  Regresar</a>
                </div>
            {!! Form::close() !!}

        </div>
    </div>
@endsection