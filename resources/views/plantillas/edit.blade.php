@extends('plantillas.admin_template')

@include('plantillas._common')

@section('header')

	<ol class="breadcrumb">
	    <li><a href="{{ route('home') }}"><span class="glyphicon glyphicon-home" aria-hidden="true"></span></a></li>
	    <li><a href="{{ route('plantillas.index') }}">@yield('plantillasAppTitle')</a></li>
	    <li><a href="{{ route('plantillas.show', $plantilla->id) }}">{{ $plantilla->id }}</a></li>
	    <li class="active">Editar</li>
	</ol>

    <div class="page-header">
        <h3><i class="glyphicon glyphicon-edit"></i> @yield('plantillasAppTitle') / Editar {{$plantilla->id}}</h3>
    </div>
@endsection

@section('content')
    @include('error')

    <div class="row">
        <div class="col-md-12">

            {!! Form::model($plantilla, array('route' => array('plantillas.update', $plantilla->id),'method' => 'post', 'files'=>true)) !!}

@include('plantillas._form')

                <div class="row">
                </div>

                <div class="well well-sm">
                    <button type="submit" class="btn btn-primary">Guardar</button>
                    <a class="btn btn-link pull-right" href="{{ route('plantillas.index') }}"><i class="glyphicon glyphicon-backward"></i>  Regresar</a>
                </div>
            {!! Form::close() !!}

        </div>
    </div>
@endsection