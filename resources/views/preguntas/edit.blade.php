@extends('plantillas.admin_template')

@include('preguntas._common')

@section('header')

	<ol class="breadcrumb">
	    <li><a href="{{ route('home') }}"><span class="glyphicon glyphicon-home" aria-hidden="true"></span></a></li>
	    <li><a href="{{ route('preguntas.index') }}">@yield('preguntasAppTitle')</a></li>
	    <li><a href="{{ route('preguntas.show', $preguntum->id) }}">{{ $preguntum->id }}</a></li>
	    <li class="active">Editar</li>
	</ol>

    <div class="page-header">
        <h3><i class="glyphicon glyphicon-edit"></i> @yield('preguntasAppTitle') / Editar {{$preguntum->id}}</h3>
    </div>
@endsection

@section('content')
    @include('error')

    <div class="row">
        <div class="col-md-12">

            {!! Form::model($preguntum, array('route' => array('preguntas.update', $preguntum->id),'method' => 'post')) !!}

@include('preguntas._form')

                <div class="row">
                </div>

                <div class="well well-sm">
                    <button type="submit" class="btn btn-primary">Guardar</button>
                    <a class="btn btn-link pull-right" href="{{ route('preguntas.index') }}"><i class="glyphicon glyphicon-backward"></i>  Regresar</a>
                </div>
            {!! Form::close() !!}

        </div>
    </div>
@endsection