@extends('plantillas.admin_template')

@include('scholarDays._common')

@section('header')

	<ol class="breadcrumb">
	    <li><a href="{{ route('home') }}"><span class="glyphicon glyphicon-home" aria-hidden="true"></span></a></li>
	    <li><a href="{{ route('scholarDays.index') }}">@yield('scholarDaysAppTitle')</a></li>
	    <li><a href="{{ route('scholarDays.show', $scholarDay->id) }}">{{ $scholarDay->id }}</a></li>
	    <li class="active">Editar</li>
	</ol>

    <div class="page-header">
        <h3><i class="glyphicon glyphicon-edit"></i> @yield('scholarDaysAppTitle') / Editar {{$scholarDay->id}}</h3>
    </div>
@endsection

@section('content')
    @include('error')

    <div class="row">
        <div class="col-md-12">

            {!! Form::model($scholarDay, array('route' => array('scholarDays.update', $scholarDay->id),'method' => 'post')) !!}

@include('scholarDays._form')

                <div class="row">
                </div>

                <div class="well well-sm">
                    <button type="submit" class="btn btn-primary">Guardar</button>
                    <a class="btn btn-link pull-right" href="{{ route('scholarDays.index') }}"><i class="glyphicon glyphicon-backward"></i>  Regresar</a>
                </div>
            {!! Form::close() !!}

        </div>
    </div>
@endsection